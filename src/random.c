#include "random.h"

uint16_t seed = 0;

void random_init(void)
{
   //init timer2
   return;
}

void random_generate_seed (void)
{
   // check CTR value
   // seed = CTR value
   return;
}

uint16_t random_generate_number (uint16_t seed)
{
   // check CTR value
   // operate combining with seed
   // update seed value
   // return num
}
